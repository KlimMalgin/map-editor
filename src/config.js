
let cfg = {
    tileData: {
        surface: 'ground',  // grouns | wall
        static: false,      // Физический объект или нет
        respawn: {          // Точка рождения юнита
            isset: false,
            teamId: 0
        }
    }
};

export default cfg;
